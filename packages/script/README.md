# @btxml/script

BT.CPP-compatible script tokenization, parsing, and analysis primitives for BTXML tooling.

- Tokenize and parse BT.CPP script attributes
- Build conservative script environments from remaps and augmentations
- Analyze read/write identifier usage and local `:=` symbol introduction
- Produce script-aware completion candidates for identifiers, enums, booleans, and operators
