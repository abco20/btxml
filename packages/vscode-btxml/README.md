# BehaviorTree.CPP XML Tools

Alpha VS Code extension for `btxml`.

## Features

- Diagnostics through the bundled `btxml` language server
- Completion, hover, definition, symbols, formatting, and code actions
- Commands for restarting the language server and formatting BT XML
